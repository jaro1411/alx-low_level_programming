#include "main.h"
#include <string.h>

/**
 * _strcmp - a function that compares two strings.
 * @s1: parameter one
 * @s2: parameter two
 * Return: an integer
 */

int _strcmp(char *s1, char *s2);
{
	int final;

	final = strcmp(s1, s2);
	return (final);
}
